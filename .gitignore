# dependencies
/node_modules
/.pnp
.pnp.*

# testing
/coverage
# screenshots from scripts/verify-site.mjs
/.verify/

# next.js
/.next/
/out/

# production
/build

# env files: never commit real secrets. Only the example is tracked.
.env
.env.*
!.env.example

# keys and certificates
*.pem
*.key

# encrypted database backups (npm run db:backup). Encrypted or not, they hold
# members' data and never belong in a public repository.
/backups/
*.zcbak

# emails written to disk instead of being sent, on the laptop only
/.outbox/

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# editors and OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Obsidian vault state (lessons are written in Obsidian; its workspace files are local)
.obsidian/

# local-only Claude Code settings
.claude/settings.local.json
